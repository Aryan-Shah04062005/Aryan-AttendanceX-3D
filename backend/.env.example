# Port configuration for Express API server
PORT=5000

# JSON Web Token Secret
JWT_SECRET=fallback-super-secret-attendance-key

# MongoDB Connection String (Optional - defaults to persistent local JSON fallback files if omitted or fails to connect)
MONGODB_URI=mongodb://localhost:27017/attendance
