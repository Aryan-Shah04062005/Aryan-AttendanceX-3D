{
  "name": "aryan-attendancex-3d",
  "version": "1.0.0",
  "description": "Futuristic 3D Employee Attendance Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n \"api,app\" -c \"purple,blue\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
